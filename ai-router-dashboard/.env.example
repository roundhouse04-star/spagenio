PORT=3000

# ── 보안 시크릿 (필수, 기본값 fallback 제거됨) ───────────
# 생성: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# ⚠️ JWT_SECRET 변경 시: 모든 사용자 재로그인 필요
JWT_SECRET=
ADMIN_JWT_SECRET=
# ENCRYPT_KEY: 64-char hex (권장) 또는 32+ char 문자열
# 변경 시 기존 user_broker_keys 복호화 불가 → 점진 마이그레이션 위해 LEGACY 같이 사용
ENCRYPT_KEY=

# (선택) 구버전 키로 암호화된 데이터 복호화용 폴백.
# 1차로 ENCRYPT_KEY 시도 → 실패 시 legacy 시도. 신규 암호화엔 사용 안 함.
# 예전에 ENCRYPT_KEY 설정 안 한 채 운영했다면 코드 기본값 사용 중 → 이 값 그대로 paste:
#   ENCRYPT_KEY_LEGACY=ai-router-encrypt-key-32chars!!
# 마이그레이션 끝나면 삭제 권장.
ENCRYPT_KEY_LEGACY=

# ── Alpaca (주식 매매) ────────────────────────────────────
# ⚠️ 키는 절대 코드에 하드코딩하지 말 것. .env 만 사용.
# ⚠️ 이전 키가 git history에 노출되었다면 즉시 폐기·재발급:
#    https://app.alpaca.markets/paper/dashboard/overview
ALPACA_API_KEY=
ALPACA_SECRET_KEY=
ALPACA_PAPER=true

# ── Python 서비스 보호 ───────────────────────────────────
# Node 대시보드 → Python(stock_server.py / quant_engine.py) 호출 시
# X-Internal-Token 헤더로 검증. mutating 엔드포인트는 이 토큰 없으면 거부.
# 생성 예: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
INTERNAL_API_TOKEN=
# 1회 주문 최대 수량 한도 (사고 방지)
MAX_ORDER_QTY=10
# Python 서비스 바인딩 호스트 (기본 127.0.0.1, 외부 노출 금지)
PYTHON_HOST=127.0.0.1
# CORS 허용 origin (콤마 분리)
CORS_ORIGIN=http://localhost:3000
