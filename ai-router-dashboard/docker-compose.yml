version: '3.9'
services:
  ai-router-dashboard:
    image: node:20-alpine
    container_name: ai-router-dashboard
    working_dir: /app
    command: sh -c "npm install && npm start"
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - ./:/app
